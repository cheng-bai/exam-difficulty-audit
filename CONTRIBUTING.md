# 贡献指南

感谢考虑为本项目贡献。本项目是**规则型**工具链，最大的价值在于口径一致、可复算，因此对贡献的第一要求是**可追溯**。

## 一、硬性红线

**请勿提交任何含第三方版权的试题原文、答案或解析。** 包括但不限于：高考真题、模拟题、教辅题目、教育网站解析。以下目录已在 `.gitignore` 中，不要强行 `git add -f`：

- `papers/`
- `out/`

新增示例请**完全自拟**，放在 `examples/` 下。判断标准很简单：这段文字如果是你原创的，就可以提交。

## 二、贡献什么最有价值

1. **新的步骤类型**（`src/step_types.py`）—— 带定义、纳入条件、排除条件三者齐全，且在真实题目上验证过不与既有类型重叠。
2. **六维打分的边界案例** —— 「这一步是 K=1 还是 K=2」这类争议的判断依据，最有价值。请给出原题条件 + 动作 + 你的判断 + 反面解读。
3. **跨卷报告模板** —— 学科特化的对比视图（`src/cross_paper.py` 之外的扩展）。
4. **其他学科的适配** —— 需要说明哪些类型不适用、哪些需要新增。
5. **正确性修复** —— 公式取整、路径搜索、统计分母等。请附最小复现。

## 三、提交前自检

```bash
python src/check_syntax.py          # 数据文件语法体检
python src/build.py --demo          # 示例必须端到端跑通
```

- [ ] `--demo` 无报错，`out/` 下四类产出齐全
- [ ] 未引入个人绝对路径（本机目录、同步盘路径等）
- [ ] 未引入第三方试题原文
- [ ] 若改动了公式或口径，已同步更新 `docs/scoring.md` 并在 `CHANGELOG.md` 记录
- [ ] 若改动了数据结构，已同步更新 `docs/schema.md`

## 四、口径变更的特别说明

**任何改变 D 值含义的修改都属于破坏性变更**，因为它会让历史结果不可比。这类修改必须：

1. 在 `CHANGELOG.md` 中显式标注为不兼容变更；
2. 保留旧口径的可选开关，或在文档中明确说明「旧结果需重跑」；
3. 说明变更依据（最好是校准数据，而非直觉）。

## 五、提交信息

中文或英文皆可，格式：

```
<类型>: <一句话说明>

<可选正文：为什么改、影响哪些文件、如何验证>
```

类型建议：`feat` / `fix` / `docs` / `refactor` / `data`（仅限自拟数据）/ `chore`。

## 六、许可

提交即表示你同意你的贡献以本项目的 [MIT License](LICENSE) 授权发布，且你有权作出该授权。
