# 变更记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。**任何改变 D 值含义的修改都属不兼容变更**，会让历史结果不可比，必须显式标注。

## [0.1.0] — 2026-09-24

首次开源发布。

### 新增

- **计算引擎** `src/build.py`
  - 六维打分与步骤负荷 `s_i` 复算
  - 关键路径搜索（依赖 DAG 上有效负荷之和最大者），含 `d_j = min(1+0.1(j-1), 1.3)` 与同型机械重复折扣 `r = 1 / 0.7 / 0.4`
  - `H / C / T / D` 与「基础—中档—难题」档位
  - 教师阅读版 Markdown 排版（口径核验 / 整卷概览 / 逐小问总表 / 正文占位符替换 / 复核清单 / 六维证据附录）
  - 机器可读产出：`评分明细.json`、`逐小问评分.csv`、`步骤类型词典.json`
  - 试卷自动发现、目录可配置、小问数一致性断言
- **跨卷对比报告** `src/cross_paper.py`（配置驱动，手写结论走外部 Markdown 附加段）
- **步骤类型词典** `src/step_types.py`：起始版 24 类（T01—T24），版本 `EDA-1.0`
- **数据体检工具** `src/check_syntax.py`
- **自拟示例卷** `examples/demo/`（不含任何第三方试题）
- **文档** `docs/methodology.md`、`docs/scoring.md`、`docs/schema.md`、`docs/copyright.md`

### 口径说明

- 取整约定：统一非负数四舍五入；**仅**在公式标明 `round` 处取整，中间量 `s`／`e` 保留精度。
- `D = round(0.45H + 0.35C + 0.20T)`；档位阈值 34 / 64。
- 小问分值未知时不做分值加权，改用小问等权并报告覆盖率；待核验单元不计入均值。

### 已知限制

- 权重与档位阈值**未经实测校准**，属规则设定；文档已多处标注，请勿当作实测难度。
- 步骤拆分粒度与六维打分含判断成分，建议引入独立复核者。
- 跨卷报告只做**同口径**比较，不混合不同口径的历史结果。

---

## 版本演进备忘（给后续维护者）

本项目的源头是一次跨四个年度的整卷难度审计实践。开源版本做了三项取舍：

1. **剥离第三方试题** —— 原实践中的试题原文、官方答案与解析全部不入库，改为「引擎 + 方法论 + 自拟示例」，试题由使用者自备。
2. **去个性化的跨卷报告** —— 原先跨卷报告中的年份、结论、教学建议均为手写；开源版改为**配置驱动 + 外部 Markdown 附加段**，让可计算的部分自动生成、判断性的部分由使用者自己写。
3. **中文为一等公民** —— 文档与代码注释以中文为主，术语保留原文。
